import { Text } from "tiny-planet";

export const Default = () => (
  <Text className="text-base text-gray-900">
    The base text primitive — style it with utility classes.
  </Text>
);

export const Muted = () => (
  <Text className="text-sm text-gray-500">A quieter, secondary line of text.</Text>
);
