import { ScreenHeader } from "tiny-planet";

export const Default = () => (
  <div style={{ width: 340 }}>
    <ScreenHeader title="Profile" onClose={() => {}} />
  </div>
);

export const NewHang = () => (
  <div style={{ width: 340 }}>
    <ScreenHeader title="New hang" onClose={() => {}} />
  </div>
);
